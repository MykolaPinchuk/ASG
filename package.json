{
  "name": "asg",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate:scenario": "tsx src/cli/validateScenario.ts scenarios/scenario_01.json",
    "match": "tsx src/cli/runMatch.ts",
    "validate:replay": "tsx src/cli/validateReplay.ts",
    "check:determinism": "tsx src/cli/checkDeterminism.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
